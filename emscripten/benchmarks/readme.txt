
These benchmarks are from emscripten's benchmark suite.

Run each by running it's main .js file. You can provide a parameter between 0 and 5 which is how much work it should do. Without a parameter, the default is 4.

